# Logic-Guard-Layer Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# SECURITY SETTINGS (IMPORTANT!)
# =============================================================================

# Admin Authentication (Required for admin features)
# IMPORTANT: Change these values before deploying to production!
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me-in-production

# Session Security
# SESSION_SECRET_KEY will be auto-generated if not set, but set it for persistence
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# SESSION_SECRET_KEY=your-secret-key-here
SESSION_MAX_AGE=3600  # Session timeout in seconds (default: 1 hour)

# CORS Configuration
# Comma-separated list of allowed origins (leave empty for same-origin only)
# Example: CORS_ORIGINS=https://yourdomain.com,https://api.yourdomain.com
CORS_ORIGINS=

# Rate Limiting
RATE_LIMIT_REQUESTS=100  # Max requests per window
RATE_LIMIT_WINDOW=60     # Window size in seconds

# =============================================================================
# LLM CONFIGURATION
# =============================================================================

# OpenRouter Configuration (Required)
OPENROUTER_API_KEY=sk-or-v1-your-key-here
OPENROUTER_MODEL=tngtech/deepseek-r1t2-chimera:free

# Optional: Override defaults
# OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
# LLM_TIMEOUT=60.0
# LLM_MAX_RETRIES=3
# MAX_ITERATIONS=5
# LLM_TEMPERATURE=0.0

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================

# Ontology Configuration
# ONTOLOGY_PATH=data/maintenance.owl

# Debug Mode (set to false in production!)
DEBUG=false

# Server Settings
# HOST=127.0.0.1
# PORT=8000
